'use strict';

let assert = require('assert');

describe('Hello World', function () {
    it('should ensure 1 is 1', function () {
        assert.equal(8, 8);
    });
		it('should ensure 1 is 2', function () {
        assert.equal(8, 2);
    });
});
